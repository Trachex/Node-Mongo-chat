module.exports = {
    app: {
        port: 3000
    },
    jwt: {
        secret: 'secret'
    },
    database: {
        uri: process.env.MONGO_URL || 'mongodb://localhost:27017/test',
        options: {
            useNewUrlParser: true,
            useUnifiedTopology: true 
        }
    }
}
